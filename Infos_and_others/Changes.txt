Options configuration:

- added synchronized Basys 3 switch decoding
- added single-player/multiplayer and four game-mode selections
- added a framed VGA options panel with live values and UART OFFLINE status
- added a multiplexed 7-segment target display
- added keyboard-controlled virtual switches to the Verilator SDL frontend
- updated FPGA constraints, source lists, testbench and documentation

Single-player game completion:

- Added the animated Pit Stop logo to the centered main menu.
- Centered and moved the PLAY and OPTS buttons below the logo.
- Added latched game settings, scoring, timers and end conditions.
- Implemented TIME ATTACK, POINT RACE, SPEED UP and BEST OF rules.
- Added a final result panel with score and timing statistics.
- Removed the BACK button from active gameplay.
- Connected live score/time to the Basys 3 seven-segment display.
- Exposed the seven-segment value in the Verilator window title.
- Added an automated controller test covering wins and losses.
- Isolated the interactive Verilator build from stale files in the old obj_dir.


MSYS2 Verilator compatibility:

- Added automatic detection of the MSYS2 GCC 16 toolchain.
- Forced the compatible libstdc++ string ABI for Verilator-generated C++.
- Used separate ABI-specific build directories to avoid stale object files.
- Applied the workaround to both the controller test and SDL simulator build.
- Made both build scripts independent of the current working directory.


Symmetric two-board UART multiplayer:

- Added 115200 baud 8N1 transmitter and receiver modules.
- Added framed packets with magic bytes, checksum, session ID, game settings,
  current score and finish state.
- Both Basys boards use the same bitstream; PLAY on either board starts both.
- Added connection waiting, heartbeat timeout and final score exchange.
- Added multiplayer WIN/LOSE/DRAW comparison and rival score on the summary.
- Added SW12 hardware diagnostics using switches, LEDs and seven-segment output.
- Added a BTNU simulated-rival finish path for full testing with one monitor.
- Assigned UART TX/RX to JB1/JB2 and enabled all diagnostic LEDs.
- Added a two-instance automated UART/session/result test.
- Added synchronized reset release and fixed out-of-range text indexing.
- Made bitstream generation stop safely instead of copying a stale .bit file.


Timing and power sign-off:

- Split the game-panel BCD, line-selection and font-address path into three
  registered stages for the 65 MHz pixel clock.
- Registered button selection before the sprite BRAM address and replaced the
  inferred DSP48 multiplication by 78 with constant shift/add arithmetic.
- Added a dedicated reset-distribution stage with a synthesis fanout limit.
- Marked the asynchronous reset button path and normal reset activity correctly.
- Enabled the Vivado post-route timing-oriented implementation strategy.
- Added detailed timing, high-fanout and advisory power reports.
- Made the build fail when routed setup slack is negative.


Synthesis warning cleanup:

- Reused the existing registered VSYNC samples in the background and logo
  pipelines instead of synthesizing equivalent duplicate registers.
- Registered all four wheel-to-mount proximity flags before the service FSMs.
- Removed the structural combinational feedback from wheel position through
  mount selection and anchor position without changing the wheel protocol.
